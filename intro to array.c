#include <stdio.h>
void main(){
	int numbers[5]; // Declares an integer array named 'numbers' with space for 5 elements.
char name[10] = {'H', 'e', 'l', 'l', 'o', '\0'}; // Declares and initializes a character array (string).
float prices[] = {10.5, 20.0, 5.75}; // Declares and initializes a float array, size determined by initialization.
	printf("%s\n", name);
	
	printf("%f", prices[1]);
}
